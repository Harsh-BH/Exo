# Generated by EXO
FROM golang:1.21-alpine AS builder

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY . .

RUN go build -o /{{ .AppName }} .

FROM alpine:latest

WORKDIR /
COPY --from=builder /{{ .AppName }} /{{ .AppName }}

ENTRYPOINT ["/{{ .AppName }}"]
